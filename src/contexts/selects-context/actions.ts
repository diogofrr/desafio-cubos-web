import type { SelectsContextActions } from '@/types/contexts/selects-context';

const actions: SelectsContextActions = {
  SAVE_GENRES: 'SAVE_GENRES',
  SAVE_LANGUAGES: 'SAVE_LANGUAGES',
};

export default actions;
