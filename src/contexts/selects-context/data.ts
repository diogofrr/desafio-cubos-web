import type { SelectsContextInitialState } from '@/types/contexts/selects-context';

const initialState: SelectsContextInitialState = {
  genres: null,
  languages: null,
};

export default initialState;
