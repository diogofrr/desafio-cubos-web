import type { ThemeContextInitialState } from '@/types/contexts/theme-context';

const initialState: ThemeContextInitialState = {
  theme: 'light',
  isLoading: true,
};

export default initialState;
